---
name: video-replication-breakdown
description: "将用户提供的、拥有使用权或获授权的视频拆解为可复刻的生成方案。适用于一比一复刻视频、替换人物或产品、复杂运镜反推、直播间快速演示、文生视频/图生视频提示词生成。重点保留逐镜头时间线、景别、机位、镜头运动、人物动作、口播台词、道具关系、转场、光线、声音和连续性；默认用0.5–0.75秒高密度抽帧显化运动细节，并把每段教程说明与对应提示词紧邻放置。"
---

# 用户固定交付协议（最高优先级）

以下规则覆盖本文件中任何冲突的默认建议，必须先执行：

1. **证据先行，提示词后写**：先读取整条视频、真实抽帧、核对关键姿态和音频，再写交付内容。不得套用常见产品广告模板，不得用合理推测覆盖未确认画面。
2. **最终提示词必须完全自洽**：投喂模型的正文严禁出现“参考视频、原视频、参考图、替换产品、原产品、保留原视频动作、承接上一段、上一段、下一段”等外部依赖或内部分析措辞。原视频和图片只用于内部建立证据账本；最终提示词直接把目标产品、场景、人物和动作写成成片事实。
3. **目标产品直接作为成片产品**：用户提供的产品就是最终产品，不写“将A替换成B”。如果用户要求换产品，内部只做映射，外部提示词只描述B及其外观、材质、比例、文字、动作和接触关系。
4. **总控提示词范围固定**：总控只写场景、人物、产品、整体风格和全局视觉约束；不得写总时长、分段方式、累计时间码或动作顺序。
5. **固定15秒分段算法**：不按自然切点，不按镜头切点，不按动作切点，也不人为寻找切点。段数严格等于 `ceil(视频总时长 / 15秒)`；第1段为源视频0–15秒，第2段为15–30秒，依此类推，最后一段到实际结束时间。不得把15秒附近的片段改拆成7秒、9秒或其他自选长度。每段投喂时间轴都从 `00:00.0` 重新开始。
6. **分段提示词必须独立完整**：每段都重复写完整的场景锁定、人物锁定、产品锁定、固定开场状态、逐0.5秒动作、镜头/连续性、声音和负面约束；不得依赖前后段文字。固定15秒边界切到动作中间时，下一段直接从该边界真实状态开始，不添加“承接上一段”等说明。
7. **真实产品结构锁定**：先确认产品的方向、结构、按钮、盖子、表链、手部接触点和空间关系，再写动作。看不清的结构不得猜成常见产品结构；交付中不补写未经证实的功能、配件或背面细节。
8. **时间轴完整覆盖**：每段按0.5秒节点展开到该段末尾，不能跳过中段、合并动作或用概括词覆盖连续运动。总时长、段数、每段源时间范围和每段本地时间范围交付前必须机械复核。
9. **一键复制格式**：最终每个片段放在独立中文代码块中，代码块内不出现外部参考语、内部证据语或未验证说明；分析证据、置信度和素材限制放在代码块外，只有用户要求时才输出。
10. **声音纪律**：只把真实可听到的人声写入口播/SRT；听不清就不补词。画面品牌、包装、屏幕文字和背景文字不能自动当成台词。
11. **最终文本禁词门禁**：交付前逐个扫描每个投喂代码块，发现“参考视频、原视频、参考图、替换产品、替换人物、原产品、原人物、保留原视频、承接上一段、上一段、下一段、参见抽帧图”等词，必须改成直接的成片事实描述。
12. **分段算术门禁**：根据实际视频秒数计算 `ceil(duration / 15)`；逐段列出源时间范围和本地时间范围；检查每段本地时间是否从`00:00.0`开始，固定边界是否只出现在0、15、30、45秒等累计15秒位置。
13. **产品门禁**：逐条检查产品形状、方向、结构、材质、文字、按钮、表链/杯体、正反面和手部接触点；任何只在常识中存在而画面未确认的结构不得写入。
14. **证据门禁**：每个0.5秒节点都必须能回到真实画面找到主体状态、动作阶段、机位/构图和连续性依据；不能用“继续、保持、自然、同上”掩盖未观察细节。
15. **输出门禁**：总控提示词不能出现动作顺序、总时长或分段说明；每个片段提示词必须独立完整、可单独复制；内部抽帧图、接触表、缓存路径和分析日志不得进入最终正文。
16. **多手连续性门禁**：逐节点追踪第一只手、第二只手及可能出现的辅助手，分别记录进入方向、持物手、接触点、交接时刻、退出时刻和是否继续辅助。出现翻面、传递、佩戴或重新握持时，必须写清“哪只手先持有、哪只手从哪里进入接住、第一只手何时释放/退出”；不得把多只手压缩成“一只手持续操作”，也不得凭左右手常识猜测未确认的手。
17. **输入身份门禁**：每次任务必须以用户本轮提供的绝对路径为唯一输入。即使文件名相同，也不得复用其他路径、旧视频、旧接触表、旧抽帧、旧音频、旧时间轴或旧分析缓存。开始处理前记录并核对绝对路径、文件大小、修改时间、时长、分辨率、帧率和视频指纹；任一项变化都必须从零重新抽帧和核对。输出中不得混入其他同名文件的场景、人物、道具、动作或时长。

# 视频一比一复刻拆解

## 目标

把用户提供的视频转换成简洁、可直接复制使用的中文复刻提示词。默认交付中文标题、总控视觉锚点、独立15秒分段投喂包、逐0.5秒代码框时间轴、真实口播对齐信息和生成负面约束。每个时间点写清画面与动作、机位与连续性要求，并保留每行前后的竖杠 `|`。

仅对用户拥有或获授权使用的视频执行复刻分析。对原视频无法确认的内容必须标注“不可确认”，不得凭空补写。

## 输入判断

1. 读取视频文件，确认时长、分辨率、帧率、画幅、是否有音轨。
2. 检查用户是否附带人物参考图、产品参考图、场景参考图或目标替换要求。
3. 若用户没有指定生成平台，使用通用视频模型格式；不要虚构某个平台的专属参数。
4. 若用户说“快速”“直播使用”，采用精简交付；仍保留所有会影响一比一复刻的运动和连续性信息。

## 抽帧策略

使用 `frame-grab-sheet` 技能生成真实抽帧总览，并按时间顺序阅读原始画面。默认：

- 默认输出节点：每0.5秒一个时间点，Frame_001 = 00:00.00。
- 常规镜头也按0.5秒抽帧；不得为了简短改成1秒一帧或按大段合并。
- 快速动作、推拉摇移、变焦、转场、手部操作、人物换位：交付仍统一使用0.5秒节点；仅在内部观察时临时加密核对，不新增0.25秒交付节点。
- 片头、片尾、场景切换、人物/产品首次出现和消失处必须补关键帧。
- 抽帧是观察工具，不是输出内容的替代品。不要只给总览图，不要用1秒一帧覆盖整条复杂视频。

内部观察时可以用镜头切换、景别变化、运镜方向变化、主体动作阶段变化、台词句意变化、场景/服装/道具变化和音乐结构变化定位证据节点；这些只是0.5秒时间轴的观察标签，不能改变最终固定15秒分段边界，也不能生成2–8秒交付片段。单个固定15秒片段内包含多个运动阶段时，必须继续按0.5秒记录，不能合并概括。

## 分析顺序

按以下顺序逐段分析，避免把所有提示词堆到文末：

1. **画面锁定**：场景、主体数量、人物外观、服装、产品、道具、空间关系、光线、色彩、画幅。
2. **镜头反推**：景别、机位高度、视角、焦段观感、构图、对焦、景深、镜头起止位置。
3. **运镜反推**：固定/推进/拉远/横移/跟拍/环绕/摇摄/俯仰/升降/手持/变焦；记录方向、速度、加减速、运动轨迹和是否有剪辑点。
4. **动作时间线**：用“起始姿态 → 过程动作 → 结束姿态”描述，包含手、头、眼神、重心、步伐、表情、人与道具接触点。
5. **口播与声音**：逐句记录可辨认台词及时间；标记对白、环境声、拟音、音乐、停顿和情绪。听不清就写“听不清”，不要猜词。
6. **连续性约束**：人物身份、服装、发型、道具数量、左右手、空间朝向、光线方向、镜头轴线、动作衔接和产品外观必须跨段保持。
7. **内部对象映射**：若用户要更换人物/产品，只在内部证据账本中记录“动作、位置、比例、遮挡、光影、镜头关系和出现时机”的继承关系；最终投喂提示词只写目标人物/产品的成片事实，不输出映射过程或“替换”措辞。

## 标准交付格式

除非用户另有要求，严格按以下结构输出：中文标题、全局复刻说明、执行规则、逐时间点分镜表。分镜表必须放在 Markdown 代码框内，原样保留每行开头和结尾的 `|` 竖杠，以及表头和分隔线中的 `|`。每个时间点必须独立写出画面与动作、机位与连续性要求；可以使用“继续揉洗”等简洁动作词，但不得用“同上”或空泛的“保持不变”替代关键细节。不要输出英文提示词、分段复刻卡、口播长篇分析或重复负面词。

### 1. 复刻结论

- 视频时长、画幅、总段数。
- 复刻难点：列出最多3项，例如复杂跟拍、遮挡交接、快速转场。
- 资产需求：人物参考图、产品图、场景图、声音/字幕文件。

### 2. 全局锁定词

只写跨全片都要保持的约束：画幅、整体风格、人物身份、服装、产品外观、光线、画面质感、动作连续性、禁止新增元素。

### 3. 逐0.5秒复刻时间轴

时间轴是最高优先级，不得合并、跳过、重排或重新创作。每个节点同时执行“画面与动作”和“机位/连续性要求”。每个节点至少写明：主体状态、动作阶段、镜头位置/运动、道具关系、光线或背景锚点；有口播时写明该节点对应台词或声音。

使用以下带竖杠的 Markdown 表格结构，按真实视频时长展开。实际任务必须覆盖全片；完整表格必须置于代码框中，避免竖杠被渲染成普通表格：

```text
| 时间 | 帧 | 画面与动作 | 机位/连续性要求 |
|---|---:|---|---|
| 00:00 | 001 | 写清主体起始状态和正在发生的动作。 | 写清机位、景别、镜头运动、背景锚点和下一节点的连续关系。 |
| 00:00.5 | 002 | 写清相对于上一节点发生的动作变化。 | 写清主体位置、手部或道具接触点、焦点和镜头变化。 |

表格内容本身就是可直接复制给视频模型的生成提示词，不再为每个镜头段重复生成英文提示词。只有用户明确要求“分段提示词”时，才额外补充分段版本。
```

时间轴提示词必须包含：主体、场景、构图、镜头运动、逐时间点动作、道具关系、连续性和负面约束；声音与口播只放在独立SRT，不写回时间轴。默认只用中文，不输出英文总控词。不要用横向超长分隔线、伪代码、空代码块或“1”占位符。若视频时长为39秒，默认应有约79个0.5秒节点；若视频时长为41.2秒，应覆盖到最后一个约0.2秒的尾节点。

### 4. 片尾验收

用简短清单检查：

- 时间线是否覆盖全片，没有跳过镜头。
- 每个镜头是否写清起止状态和镜头运动。
- 每个动作是否能在视频模型中执行，而非抽象形容词。
- 台词和声音是否与时间对齐。
- 替换对象是否只替换指定对象。
- 各段首尾是否有可复用的连续性锚点。

## 提示词写法规则

- 先写“保持什么”，再写“发生什么”，最后写“禁止什么”。
- 复杂运镜必须写成路径，例如“从胸部中景开始，镜头向右横移并跟随人物，人物经过前景遮挡后镜头继续推进至产品近景”。
- 不把“快速、电影感、自然”当作完整动作；必须补充方向、速度、轨迹、主体位移和结束位置。
- 不把多段动作压成一句；按时间顺序写每个动作阶段。
- 文生视频提示词必须自包含；最终投喂正文不能写“参见抽帧图、参考视频、参考图、上一段、下一段”等外部依赖。用户提供的产品/人物直接作为最终成片主体描述。
- 若用户提供了产品或人物图片，内部只用来建立外观锚点；最终提示词直接描述目标主体，不使用“替换人物、替换产品、原人物、原产品、保留原视频”等映射措辞。
- 不能保证模型生成“像素级一模一样”；交付目标是镜头结构、动作节奏、运镜轨迹和视觉关系尽可能一致，并指出需要首尾帧或后期合成的部分。

## 交互与输出控制

- 用户只说“拆解/复刻”：输出中文简化版，即全局复刻说明、执行规则和逐0.5秒时间轴表；减少背景科普。
- 用户提供“逐0.5秒生成视频提示词”示例：严格仿照示例结构，只使用中文代码框、逐0.5秒节点和竖杠格式；时间轴不写台词，口播只在独立SRT中呈现，不增加英文提示词或重复的分段说明。
- 用户说“只要提示词”：保留每段标题、时间、提示词和一句衔接约束，隐藏长篇解释。
- 用户说“直播快速展示”：优先输出可复制提示词、资产清单和3项验收点；不要输出空代码块。
- 用户说“复杂运镜”：自动提高抽帧密度，增加镜头轨迹、焦点变化、遮挡关系和首尾锚点字段。
- 用户要求产品/人物变化：内部完成映射，外部交付只输出目标主体的完整锁定描述和对应动作；禁止把映射过程写进最终提示词。

## 场景语义与关系建模（所有视频必须执行）

在写时间轴或提示词前，先建立“场景—人物—物体—动作—台词—剪辑目的”关系，不得只做逐帧物体识别。每个连续片段先回答：谁在做什么、对什么做、为什么做、动作结果是什么、下一镜如何承接。

### 关系建模规则

- 为每个主要人物建立稳定身份：外观、服装、发型、姿态、所在空间和与道具的关系；切镜后先判断是否仍是同一人物、同一场景和同一动作链。
- 为每个道具建立状态链：未拿取 → 拿取 → 操作中 → 展示结果 → 放下/交接；不得把“展示结果”误判为新事件或新人物。
- 将镜头分为功能：铺垫/提出问题、动作执行、结果展示、反应/强调、回扣/继续台词、转场。相邻镜头若服务同一表达，必须写成一条完整语义链。
- 台词必须绑定“说话时人物的动作和镜头功能”。音频跨切镜时按真实时间拆分；不得把整句台词归给第一个镜头，也不得因为画面没有明显口型就把台词移到下一个镜头。
- 对“切回开头”“继续”“这才是”“如果……那么……”等语义信号，优先检查前后镜头的姿态、道具状态和表达目的是否回扣，而不是只描述画面变化。

### 场景关系核验表

交付前逐段核验五项：

1. 人物身份是否连续；
2. 人物与空间的关系是否连续（站、坐、躺、床面/桌面等）；
3. 道具是否从上一镜的状态自然过渡；
4. 当前动作是否是台词的铺垫、执行、结果或强调；
5. 下一镜是否承接当前表达，而不是凭画面相似度强行合并。

若这五项中有一项无法确认，回看该段切点前后更密集的画面，并在输出中标注不确定性，不得用概括性词语掩盖关系缺失。

## 15秒固定分段投喂模式

当用户要求“一键复制”“直接使用”“复刻视频”或未指定其他分段长度时，必须采用固定15秒分段协议：

- 段数严格计算为 `ceil(视频总时长 / 15秒)`。
- 固定源时间边界为0、15、30、45秒，不能改用自然停顿、镜头切换、语义转折或动作完成点作为分段依据。
- 第1段源时间为0–15秒，第2段为15–30秒，依此类推；最后一段到视频真实结束时间。
- 每段的投喂时间轴从`00:00.0`重新开始，不能沿用累计时间码。
- 不得为了“自然”把一段改成7秒、8秒、9秒或其他长度；不得把短尾段并回上一段，也不得为了凑满15秒补写不存在的画面。
- 固定边界切到动作中间时，下一段直接从该时间点真实存在的主体姿态、手部位置、道具状态和镜头状态开始；不要写“承接上一段”等外部说明。

每个独立投喂包必须包含：该段完整场景/人物/产品锚点、固定开场状态、0.5秒视觉时间轴、声音要求和负面约束。总控提示词另行提供且只锁定场景、人物、产品和整体风格。视觉时间轴不写台词；有真实口播时，紧邻附上对应SRT。没有可辨认口播时明确写无口播，不虚构台词。默认输出一键投喂包、独立SRT（如有）和整片合并顺序；不输出抽帧图或内部核验材料。

## 反推提示词自检与迭代升级

生成复刻提示词前，必须先从视频证据反推出画面的生成机制，而不是把看到的表面现象直接改写成提示词。每个0.5秒节点都要在内部确认五层关系：主体是谁、主体位于什么空间容器、背景/遮罩由什么构成、道具和动作服务什么主题、下一个时间节点如何连续。

### 背景与空间拓扑检查

- 看到白色区域时，先判断它是独立白色背景、被放大的产品/物体表面、白色抠像轮廓、图形遮罩，还是场景中的真实墙面。
- 如果白色区域在后续显露出稳定的瓶形轮廓，且人物始终位于其内部，优先反推为“白色瓶形空间被放大后逐渐显露”，不得写成“白色背景切换为瓶子背景”。
- 提示词必须写清前景主体、中央容器、后方品牌图形和绿色底层的层级关系；禁止只写“白色背景、绿色背景”这种会丢失拓扑的概括。

### 服装变化与主题触发检查

- 若服装、发型、姿态和周围道具在同一转场中整体改变，默认判断为新的主题造型卡片，不描述为人物在镜头内连续换衣服。
- 每套造型必须绑定一个可观察的生活方式主题，并由对应道具、动作和画面节奏共同证明；不得只凭颜色猜测主题。
- 服装变化的触发因素优先写为“图形转场/音乐节拍/主题卡片切换”，除非原片明确表现出人物自行换装或剧情导致换装。

### 提示词反推闭环

完成初稿后，逐段反问：

1. 这句话描述的是实际观察到的画面，还是我补写的因果解释？
2. 背景到底是平面、物体表面、遮罩还是容器空间？
3. 人物是连续动作，还是同一广告主题下的多个造型卡片？
4. 产品与人物的手部、嘴部和空间位置是否在每个变化段保持正确？
5. 服装变化是否有对应的道具、动作和主题证据？
6. 下一镜是动作承接、结果展示、主题切换还是产品收束？

如果提示词无法回答其中任一项，先回到对应时间段重建场景关系，再改写提示词。最终提示词必须通过“画面结构—人物关系—道具主题—转场原因—收尾版式”五项一致性检查。

## 内部证据与最终交付分离

抽帧图、接触表、运动总览图、缓存帧目录和检测日志只允许作为内部核验材料，禁止出现在最终回复、最终拆解正文或交付链接中。最终输出必须严格沿用已确认的交付结构：总控复刻提示词、0.5秒视觉时间轴、负面提示词、分段生成方法、资产/连续性要求和独立SRT；时间轴只写画面与动作，不写台词。除非用户明确要求查看证据图，否则不得展示或引用任何抽帧图。

最终回复发送前执行格式门禁：删除所有抽帧图、接触表、图片Markdown、抽帧文件链接、缓存路径和内部检测过程；核对正文只包含约定的六个交付部分。若用户说“按前面一样”，以前面已确认的输出结构为最高优先级，不得被工具默认交付格式覆盖。

## 工具协同

- 本 Skill 不负责生成抽帧图、接触表或运动总览图；用户如需这些内容，单独调用 `frame-grab-sheet`。
- 语音转写或字幕提取：使用本地可用的转写工具；输出必须标注置信度或不可确认部分。
- 需要Word文档：调用 `documents:documents`，将每段教程与提示词放在同一节，并渲染检查排版。
- 不要为了“看起来完整”生成虚假的下载链接、空代码块或未验证的工作流文件。

## 一键全量拆解流程

当用户只说“拆解视频”“复刻视频”或“按这个视频拆解”时，默认一次性执行以下闭环，不再只输出镜头表：

1. 读取视频元数据：时长、分辨率、帧率、画幅、音轨、声道和采样率。
2. 直接对视频进行全片视觉理解；整条视频从头到尾按0.5秒均匀理解和输出，不因视频变长而降低中后段细节密度。快速动作、转场、产品首次出现/消失、手部遮挡和发型变化处，仅在内部临时加密观察，最终仍回填到0.5秒节点。本 Skill 不生成或交付抽帧图、接触表或运动总览图。
3. 抽取音频并进行中文语音转写。优先使用本地可用的 `faster-whisper` 或同等本地语音识别工具；音频路径含中文时先复制到ASCII临时路径，避免转写工具乱码。
4. 只转写真实可听见的人声口播，不读取画面文字作为台词，不把包装文字、画面字卡、背景音乐歌词或环境声写入口播。
5. 为每句口播生成标准SRT：序号、`HH:MM:SS,mmm --> HH:MM:SS,mmm`时间码、原始口播、`[emotion: xxx]`情绪标签。按自然语义和停顿切分，每条建议1—3秒，最长不超过5秒，不重叠、不跳跃。
6. 0.5秒镜头时间轴只写画面、主体动作、机位、运镜、道具、光线和连续性，不重复写台词/口播；所有人声统一放入独立的标准情绪SRT。只有真实人声无法辨认时才写`[inaudible]`，不得用“听不清”作为默认占位。
7. 输出全局复刻提示词、逐节点时间轴、标准情绪SRT、负面提示词、资产清单、复刻难点和片尾验收。

## 直播深检合并模式

直播和深度核验不做二选一。默认采用“双通道并行 + 统一深度验收”：用并行和缓存缩短等待，用全片覆盖和反向复核保证准确性。

### 通道A：快速全片底稿

1. 读取元数据并计算视频指纹；同一视频已有抽帧、转写、场景和差异结果时直接复用缓存。
2. 并行执行：全帧机器级读取与相邻帧变化扫描、全片0.5秒节点抽取、音频转写、场景切点候选检测。
3. 立即建立从首帧到尾帧的0.5秒画面账本，不能只做开头、结尾或少数代表帧。

### 通道B：准确性深检

1. 对通道A生成的每个0.5秒节点逐项核对主体、动作阶段、构图、机位、运镜、道具、光线、背景和前后状态。
2. 对所有切点、快速动作、手部遮挡、焦点变化、人物/服装/发型变化、产品首次/最后出现和描述置信度不足区间自动加密观察。
3. 至少进行两次反向校验：先由画面生成时间轴，再从时间轴逐节点回查原帧；音频SRT独立生成后再与画面动作和剪辑切点对齐。
4. 发现冲突时回到原始帧重建该区间，不能用旧摘要、旧时间轴或“合理推测”覆盖新证据。

### 直播交付节奏

- 内部同时跑通道A和通道B；只有通道B完成关键验收后才交付最终版本，不为了快而先发未经核验的半成品。
- 对外输出保持前面约定的格式：全局复刻提示词、全片0.5秒画面时间轴、独立情绪SRT、负面提示词、资产清单和验收结果。
- 输出正文精简，隐藏过程日志；只报告已核验的结果、关键风险和必要的素材限制。
- 若视频特别长，按时间区间分批并行处理，但每批都必须经过同一套画面账本和反向验收，最后再按原时间顺序合并。

### 速度与准确性的硬约束

- 速度来自并行处理、缓存复用、批量接触表和自动切点扫描；不来自减少全片0.5秒节点。
- 深度来自全片覆盖、风险区间加密、两次画面反查和独立音画对齐；不把一张总览图当作全片理解。
- 任何一个中段连续区间无法确认，都必须暂停最终交付并补检该区间。
- 内部可以使用更密帧，但最终输出永远保持0.5秒节点，不增加0.25秒交付节点。

## 计算资源调度

用户已授权在视频拆解任务中使用当前环境全部可用计算资源。执行时按以下规则调度：

- 自动检测逻辑CPU核心数、可用内存和GPU；不要假设GPU一定存在。
- 有CUDA GPU时，中文语音转写优先使用GPU；根据显存选择合适模型和半精度计算，避免无谓降级。
- 无GPU或GPU不可用时，使用全部可用CPU核心进行转写、抽帧、帧差扫描和接触表生成。
- 将全帧扫描、0.5秒抽帧、音频提取/转写、场景切点检测拆成并行任务；长视频按时间区间分批并行。
- 为每个任务设置资源边界，避免多个任务同时抢满内存导致整体变慢；转写和视频解码优先，接触表拼接随后执行。
- 生成缓存时记录视频指纹、资源配置、模型、抽帧间隔和转写参数；相同输入优先复用，不重复消耗计算资源。
- 计算资源只用于提高处理速度和核验覆盖，不降低最终的全片0.5秒输出标准，也不跳过深度反向验收。

## 语音转写与情绪SRT规则

- 必须以音频为唯一台词来源；画面文字只能用于核对产品名或专有名词，不能替代听写。
- 复刻生成提示词默认禁止生成画面字幕、自动文字、台词文字、字卡和贴纸；SRT只作为独立的外部语音时间文件交付，不写入生成画面。
- 尽量保持原始口语、语气词、重复和不完整表达，不润色、不总结、不翻译。
- 对ASR结果进行人工语义校正，但不得凭空补写；产品名、草本名和品牌名同时参考音频与画面，仅在两者都无法确认时使用`[inaudible]`。
- 每条SRT末尾添加情绪标签，限定使用：`neutral`、`excited`、`persuasive`、`anxious`、`confident`、`humorous`、`urgent`、`disappointed`、`satisfied`、`curious`。
- 用户要求“只要SRT”时，只输出标准SRT，不输出标题、解释、分析、Markdown围栏或其他内容。
- 用户要求“拆解/复刻”但未限定格式时，完整输出：复刻结论、全局复刻提示词、0.5秒逐节点时间轴、标准情绪SRT、负面提示词、资产清单和验收结果。

## 一键交付验收

- SRT覆盖全部可听人声，时间码与音频对齐，格式可直接导入剪辑软件。
- 时间轴覆盖全片，不跳过普通镜头；快速动作和转场已加密。
- 每个时间节点写清主体状态、动作阶段、镜头位置/运动、道具关系、光线或背景锚点；不在时间轴中重复台词。
- 独立SRT只保留真实人声口播；不把包装、字卡或后期文字误当成口播。
- 负面约束必须加入“无画面字幕、无自动文字、无文字贴纸”；除非用户明确要求，否则不要让视频模型生成任何文字层。
- 最终检查人物、服装、发型、左右手、产品形状、空间朝向、光线、道具数量和前后镜头衔接。

## 证据优先的强制复核

不得先凭总览图或印象写完整时间轴，再用抽帧补解释。必须先建立“镜头证据账本”，再生成交付内容：

1. **全片均匀复核**：从首帧到尾帧按0.5秒逐节点理解，不只检查开头、结尾或少数转场；每个节点都必须有真实画面依据。
2. **开头和片尾重点复核**：开头4秒和最后3秒必须额外确认主体姿态、道具、动作起止、转场和收尾状态，但不得因此忽略中段。
3. **切点复核**：用场景变化、构图突变、服装/发型变化、主体动作重置和产品首次/最后出现定位切点；切点前后可在内部临时查看更密帧，交付仍保持0.5秒节点。
4. **全片动作复核**：手部操作、快速甩头、吹风、遮挡、推拉摇移和焦点变化必须在发生区间内逐节点描述；不能用一个概括句覆盖一整段动作。
5. **音画独立复核**：先得到带时间戳的音频转写，再单独确认画面；不得为了让台词顺口而改写原音，也不得因为画面文字清楚就把它当作口播。
6. **新证据覆盖旧摘要**：重新抽帧或重新转写后，必须以新证据重建对应段落，不能沿用上一版概括、旧时间轴或旧镜头编号。
7. **交付前反向抽查**：随机抽查开头、每个切点、产品特写、最复杂动作和片尾；逐项问“这句话能否在对应真实帧中找到”，找不到就改写或标注不确定。

## 防止明显错误的停止条件

- 首帧、开场钩子、第一件道具或第一句口播画面无法确认时，停止输出完整复刻提示词，先补密集抽帧。
- 视频中段任意连续节点无法确认时，同样停止交付并补充该区间观察；不能只修开头和结尾。
- 镜头切点前后主体姿态无法衔接时，停止合并镜头，拆成独立节点。
- 画面概括与真实帧冲突时，以真实帧为准，禁止用“合理推测”覆盖观察证据。
- 任何时间节点出现“继续、保持不变、同上”等空泛描述时，补写该节点的具体姿态、手部路径、道具位置和镜头状态。
